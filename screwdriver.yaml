shared:
   image: node:20
   annotations:
       screwdriver.cd/virtualJob: true

jobs:
    simple:
        steps:
            - echo: echo test
        requires: [~commit]
    parallel:
        steps:
            - echo: echo test
        requires: [simple]
    join:
        steps:
            - echo: echo test
        requires: [parallel, sd@7020:external]
